--truncate table dbo.Iterations

SELECT
    *
FROM dbo.stats_Database
WHERE
    IterationId = 1

SELECT
    *
FROM dbo.stats_Database
WHERE
    IterationId = 2

SELECT
    *
FROM dbo.stats_Database
WHERE
    IterationId = 3

SELECT
    *
FROM dbo.stats_Database
WHERE
    IterationId = 4
   

select * from dbo.Iterations